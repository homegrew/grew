# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Go
permissions:
  contents: read
  pull-requests: write
on:
  pull_request:
  push:
    branches: [ "main" ]

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: true
      - run: go build ./...
      - run: mkdir -p coverageFiles
      - run: go test -race -coverprofile=./coverageFiles/coverage_atomic.txt -covermode=atomic ./...
      - run: go test -coverprofile=./coverageFiles/coverage_count.txt -covermode=count ./...
      - uses: codecov/codecov-action@v6
        with:
          directory: ./coverageFiles
          token: ${{ secrets.CODECOV_TOKEN }}
