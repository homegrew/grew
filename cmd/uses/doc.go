// Package uses implements the 'uses' command.
//
// # Show installed formulae that depend on another formula
//
// Show installed formulae that directly depend on the specified formula.
// This is the inverse of dependency listing and helps identify what would
// be affected by changes to a package.
package uses
